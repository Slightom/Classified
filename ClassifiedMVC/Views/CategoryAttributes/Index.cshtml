@model IEnumerable<ClassifiedMVC.Models.CategoryAttribute>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Attribute.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Category.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Attribute.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Category.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CategoryAttributeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CategoryAttributeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CategoryAttributeID })
        </td>
    </tr>
}

</table>
